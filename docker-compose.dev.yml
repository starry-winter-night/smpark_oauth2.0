version: '3.8'

services:
  oauth:
    build:
      context: .
      target: development
    image: smpark/oauth2.0:${VERSION}-dev
    container_name: smpark_oauth2.0_${VERSION}-dev
    ports:
      - '4000:4000'
    env_file:
      - .env.development
    environment:
      - NODE_ENV=development
    volumes:
    - .:/usr/src/oauth2.0
    - smpark_log:/usr/src/oauth2.0/src/log
    command: yarn dev
    
networks:
  default:
    name: smpark_oauth_network_dev

volumes:
  smpark_log:
